{
  "name": "verity-reputation-passport",
  "version": "1.0.0",
  "private": true,
  "description": "Wallet-connected soulbound reputation passports with a safe local demo mode",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "chain": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "test:contract": "hardhat test",
    "check": "npm run compile && npm run test:contract && npm run build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.26.3",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@clerk/clerk-js": "^6.29.2",
    "ethers": "^6.15.0",
    "qrcode": "^1.5.4"
  }
}
